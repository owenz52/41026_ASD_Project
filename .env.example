# To run the agentic loop service, create a `.env` file in the project root and add the following line.
# Replace `your_github_token_here` with your own GitHub personal access token.
GITHUB_TOKEN=your_github_token_here